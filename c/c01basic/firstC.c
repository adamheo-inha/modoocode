// https://modoocode.com/6 ~ 2-2
// comment주석은 좋은 코드의 완성이다.
// STanDard Input Output이라는 headfile을 include(포함)한다.
#include <stdio.h>
//INTeger정수값을 반환한는 main함수를 정의한다.
            //{}brace중괄호를 통해 앞의 함수 main의 영역을 알린다.
int main() {
    /*
    stdio.h로 모니터 출력이 정의된 함수 printf를 사용하여 내용을 출력한다.
    ""double quotation mark로 출력할 내용을 묶는다.
    \escape character와 n의 조합으로 개행문자 줄바꿈을 해준다.
    ;semicolons으로 하나의 명령어 문장이 끝났음을 알린다.
    */
    printf("Hello, World! \n");
    //main함수에 0을 반환하여 정상 종료 되었음을 알린다.
    return 0;
}